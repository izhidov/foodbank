flyway:
    enabled: true

server:
    port: 8888

spring:
    datasource:
        url: "jdbc:mysql://127.0.0.1:3306/fp_test?allowMultiQueries=true"
        username: app
        password: password

logging:
    config: classpath:config/logback-spring.xml